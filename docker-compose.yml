version: "3.8"
services:
  front:
    container_name: front
    build:
      context: .
      dockerfile: ./Dockerfile
    tty: true
    volumes:
      - ./src:/app
    environment:
      - WATCHPACK_POLLING=true
    ports:
      - "3000:3000"
